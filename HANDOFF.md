# HANDOFF — 힉스필드 광고영상 제작 지원 프로젝트

> 새 세션은 이 대화의 기억이 없습니다. 새 세션 첫 메시지에 이 파일 링크를 주고 "이 HANDOFF.md 읽고 이어서 작업해줘"라고 하면 그대로 이어집니다.
> 원본: https://github.com/tnaks6325-web/ad-studio/blob/main/HANDOFF.md

## 1. 프로젝트 개요

비개발자(콘텐츠 크리에이터)가 힉스필드(Higgsfield) MCP + Claude로 고퀄 광고/숏폼 영상을 만들도록 돕는 프로젝트. 산출물은 (A) 대화형 제작을 돕는 **Claude 스킬**, (B) 폼으로 프롬프트를 만드는 **웹 지원센터**.

## 2. 살아있는 결과물 (외부 저장, 영구 유지)

| 항목 | 위치 |
|------|------|
| GitHub 저장소 | https://github.com/tnaks6325-web/ad-studio |
| 라이브 사이트 | https://tnaks6325-web.github.io/ad-studio/ |
| 배포 파일 | 저장소의 `index.html` (단일 파일, vanilla JS, ~709줄) |
| 스킬 원본(SoT) | 저장소의 `skill/ad-video-helper/` (SKILL.md + references/ 4개 + assets/ 1개) |
| 생성 이미지 | 힉스필드 CDN. base URL(PVB) = `https://d8j0ntlcm91z4.cloudfront.net/user_3FZXLeq7PhSR6ydqZofct8uuWjg/` |

## 3. 힉스필드 MCP

- 커넥터 URL: `https://mcp.higgsfield.ai/mcp` (끝에 `/mcp` 필수)
- 서버 ID: `2f7cb3c4-895e-4298-baf8-3cc25c20e09d`
- 플랜: Plus (유료). 영상 생성은 유료 플랜 필수, 이미지는 무료도 가능.
- **이미지 생성**: `generate_image`, 인자는 `{params:{...}}`로 감싼다. 모델 `nano_banana_pro`(응답엔 `nano_banana_2`로 표기), `aspect_ratio:"1:1"`.
- **이미지→이미지(레퍼런스 고정)**: `params.medias:[{role:"image", value:"<job_id>"}]` + 프롬프트. "레퍼런스의 제품/인물을 그대로 유지, X만 변경" 식으로 지시하면 동일 피사체에 조명·스타일만 바뀐다.
- 생성 전 비용 확인(`get_cost`)하고 승인받는 게 원칙(스킬 규칙).

## 4. 웹사이트 구조 (index.html)

9개 탭: 시작하기 / 프롬프트 빌더 / **프롬프트 번역기** / 카피·자막 / 씬플래너 / 내 라이브러리 / 모델·비용 / 레퍼런스 재현 / 체크리스트. localStorage로 상태 유지.

핵심은 **프롬프트 번역기 탭**: 8개 카테고리(인물·배경·보이스·말빠르기·카메라·조명·스타일/포맷·비율길이)에서 옵션을 고르면 상세 영문 프롬프트로 확장. 각 행 우측에 선택 옵션의 **예시 이미지 미리보기**가 뜬다.

- 미리보기 매핑 객체: `var PREVIEW={camera:{...}, light:{...}, person:{...}, style:{...}, bg:{...}}` — 라벨→이미지URL. **여기가 미리보기 이미지의 단일 진실원본(SoT)**.
- 카테고리별 이미지 수: person 20, bg 20, camera 20, light 20, style 20 (= 100장) + 초기 실험분.
- 오디오(voice/pace)는 **패스**: 프리셋 보이스가 전부 외국인 영어톤이라 한국어에 안 맞고 끝음 하강도 못 고쳐서 사용자가 뺐음. `AUDIO` 객체는 `voice_off`라는 무효 키로 비활성화(죽은 데이터). 되살리지 말 것.

## 5. 미리보기 이미지 생성 기준 (재작업 시 필수)

같은 카테고리 안에서는 **피사체를 고정하고 해당 요소만 바꿔야** 옵션 차이가 보인다.

### 조명 (light) — 인물 고정, 조명만 변경
- 베이스 인물: job_id `0294c89b-4952-4736-ae75-712730cee79f` (파일 `hf_20260702_141521`)
- 컨셉: 앉아있는 20대 한국인 뷰티 인플루언서, 가슴이 화면 하단을 채우는 **근접 프레임**, 갸름한 V라인, 밝은 피부.
- 20종 조명을 이 베이스에 i2i로 재조명. 파일 시간대 `hf_20260702_1455~1501`.

### 스타일·포맷 (style) — 하이브리드 (사용자 승인)
"미스트 병만 고정"은 언박싱·GRWM 같은 액션 포맷을 구분 못 함 → 둘로 나눔:
- **렌더링 스타일 10종** = 분사형 미스트 병 고정, 스타일만 변경. 베이스 제품 job_id `d9c7284a-1b7e-43f0-b54b-a3d15f190680` (`hf_20260702_152401`).
  - 해당: 시네마틱 광고·하이퍼모션 제형·럭셔리 브랜드 필름·Y2K 레트로·스톱모션·3D 렌더 CG·애니메이션풍·필름 그레인 빈티지·미니멀 감성·광고 시즐샷
- **액션/포맷 10종** = 미스트 쓰는 동일 인물 고정, 포맷별 장면. 베이스 인물+미스트 job_id `92c98d35-9533-4964-aca4-0e04ddadb7e5` (`hf_20260702_152403`).
  - 해당: UGC 리얼 후기·언박싱·튜토리얼·비포&애프터·GRWM·제품 리뷰·밈/유머 숏폼·ASMR 감각·다큐멘터리·스트리트 브이로그
- 20종 파일 시간대 `hf_20260702_152611~152724`. 정확한 라벨→URL 매핑은 index.html의 `PREVIEW.style`에 있음.

## 6. 배포 절차 (GitHub Pages) — 중요

로컬에서 index.html을 고친 뒤:

1. 저장소 upload 페이지(`/upload/main`)에 로컬 `index.html`(또는 작업 파일)을 업로드 → commit.
2. 파일명이 index.html이 아니면: 기존 `index.html` **삭제** 커밋 → 업로드한 파일을 **index.html로 rename** 커밋. (rename은 편집 화면에서 파일명 필드를 index.html로 바꾸고 커밋)
3. Pages 빌드가 자동 실행되어 라이브 반영.

### 잘 막히는 지점 (실제로 겪음)
- **Pages deploy 단계 타임아웃 실패**가 종종 남 (build는 성공, deploy에서 "Timeout reached, aborting!"). 이때 라이브가 옛 내용 그대로임. → **해결: 아무 트리비얼 커밋(예: `.nojekyll` 생성)을 새로 push하면 새 빌드가 즉시 성공 배포**되며 막힌 배포를 대체함. (이번에 `.nojekyll` 이미 추가해둠 — 삭제하지 말 것)
- 검증은 브라우저에서 `fetch(url+'?cb='+Date.now(),{cache:'no-store'})`로 origin을 직접 읽어라. `web_fetch`나 그냥 새로고침은 CDN 캐시 때문에 옛 내용을 보여줘 오판하기 쉬움.
- GitHub 파일명 필드가 타이핑을 안 먹으면 triple_click으로 전체선택 후 입력.

### 세션 샌드박스 장애 주의 (실제로 겪음)
- bash 마운트와 파일툴(Read/Write/Edit) 뷰가 어긋날 수 있음(과거에 bash에서 7바이트 stale 파일을 봄). **파일 내용 작업은 Read/Write/Edit 파일툴만 신뢰**하고, bash로 cp/편집하지 말 것.
- **샌드박스 VM 자체가 부팅 실패**할 수도 있음(bash 전멸 "VM service not running" + 존재하지 않는 하위폴더 Write 거부 + 컴퓨터제어 승인창 무응답이 동시에 옴). 세션 내 복구 안 되면 **앱 재시작 후 새 세션**이 정답. 시간이 지나 VM이 늦게 살아나는 경우도 있으니 새 메시지 시점에 한 번 재확인.
- outputs 마운트에서 `zip` CLI가 마지막 rename 단계에서 "Operation not permitted"로 실패할 수 있음 → **python zipfile로 우회**. outputs 파일 삭제는 `allow_cowork_file_delete` 승인 후 가능.

## 7. Claude 스킬 (ad-video-helper)

- **설치 완료 (2026-07-03).** 저장소 `skill/ad-video-helper/`(SoT)를 받아 `.skill`로 패키징 → present_files "Save skill" 버튼으로 설치됨. 설치 후 세션 스킬 목록에 `ad-video-helper`로 뜨는 것 확인함.
- 모드 A(제품→광고영상), 모드 B(레퍼런스 영상→재현 프롬프트) 2개.
- **재패키징 절차(검증됨)**: raw.githubusercontent에서 6개 파일 다운로드 → git blob SHA-1로 무결성 검증(`(printf 'blob %d\0' size; cat file) | sha1sum`) → SKILL.md가 **아카이브 루트**에 오도록 zip → `.skill` 확장자로 present_files 전달. 스킬 파일 수정은 저장소에서 하고, 수정 시 재패키징+재설치 필요.
- 핵심 규칙: 매 단계 추천+승인, 크레딧 드는 생성 전 비용 게이트, 조용한 재시도 금지, 기본 15초(최대 30초 체이닝), 프리미엄 모델 기본(Seedance 2.0 / 시네마틱은 Cinema Studio 3.0·Veo 3.1).
- **미검증**: 새 대화에서 자연 트리거("광고 만들어줘")로 S0→S6 실동작 테스트는 아직 안 함. 테스트 시 비용 게이트 확인까지만 하고 생성 전에 멈추는 것 권장.

## 8. 완료 / 미완

**완료**: 웹사이트 제작·배포, 번역기 20종×5카테고리 옵션 확장, 미리보기 이미지 시스템, 조명 20종(고정 인물), 스타일 20종(하이브리드), 스킬 문서/패키지, 카피·씬플래너·라이브러리 기능, **스킬 .skill 패키징·설치(2026-07-03)**.

**미완/후보 작업**:
- 스킬 실동작 테스트: 새 대화에서 자연 트리거 → S0 잔액 확인 → 비용 게이트까지 흐름 검증(생성 전 중단).
- 원한다면 person·bg 미리보기도 조명/스타일처럼 "고정 기준" 재정비 가능(현재는 카테고리 특성상 각기 다른 피사체라 문제 없음 — 실사용 불만 나올 때만).
- 오디오는 의도적으로 패스 상태.

## 9. 사용자 선호 (작업 스타일)
- 항상 한국어. 결론 먼저, 표·체크리스트 우선, 장황함 배제.
- 무비판 동의 금지: 먼저 약점·반론을 짚고, 정말 타당할 때만 동의.
- 개발 작업은 운영 서비스 가정, 위험요소·롤백 먼저 설명, 변경 최소화.
